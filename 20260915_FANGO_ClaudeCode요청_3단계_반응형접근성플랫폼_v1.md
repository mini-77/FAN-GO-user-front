# Claude Code 요청 — 3단계: 반응형 · 접근성 · 플랫폼 정책

작성일: 2026-09-15
범위: CSS 토큰이 아니라 **앱 설정·네이티브 레이아웃 영역**일 가능성이 높은 항목. 1·2단계와 분리해서 별도로 요청하는 것을 권장합니다.

## 요청 문구

우리 안드로이드 앱 프로젝트에 아래 정책들이 반영되어 있는지 확인해줘. 각각 어느 파일(예: `AndroidManifest.xml`, `build.gradle`, 레이아웃/테마 리소스 등)에서 확인·수정해야 하는지 먼저 알려주고, 확인 후 하나씩 반영해줘.

1. **최소 지원 버전(minSdk)**: API 26 (Android 8.0) 이상인지 확인. `build.gradle`의 `minSdkVersion`(또는 `minSdk`) 값 확인.
2. **Edge-to-Edge 대응**: 상태바·하단 제스처 바를 투명 처리해서 콘텐츠가 시스템 바에 가려지지 않는지 확인.
3. **하단 고정 버튼·내비게이션 안전영역**: 화면 하단 고정 영역(CTA 버튼, 하단 내비게이션)에 기기의 하단 제스처 바 높이만큼 안전영역(safe-area / WindowInsets) padding이 추가되어 있는지 확인. 없다면 추가해줘.
4. **다크모드**: 앱이 자체적으로 다크모드를 지원하지 않고, 기기 시스템 설정과 무관하게 항상 라이트 테마로 고정되는지 확인. 다크모드 관련 리소스나 분기 로직이 있다면 알려줘 (제거 여부는 상의 후 결정).
5. **폰트 스케일 대응**: 기기 글자 크기 설정을 100~130%까지 레이아웃 깨짐 없이 대응하는지 확인. 130% 초과 시 더 커지지 않도록 상한(clamp) 처리가 되어 있는지 확인.

각 항목은 코드 확인 결과만 먼저 보고해주고, 실제 수정이 필요한 부분은 항목별로 어떻게 고칠지 설명한 뒤 확인받고 진행해줘.
