# FAN:GO 공통 디자인 가이드 — 1~10페이지 분석

- 원본 파일: `260913_FANGO_공통_디자인_가이드_v1 1.pdf` (사용자 Downloads 폴더, "이전에 전달한 v1 PDF")
- 총 페이지: 43p (이 문서는 그중 **1~10페이지만** 다룸. 11페이지 이후는 미분석)
- 확인 방법: PyMuPDF로 각 페이지를 2.5배율 PNG로 렌더링한 뒤 이미지를 직접 육안으로 확인함
- 코드/디자인 수정 없음 — 이 문서는 내용 기록 전용

> **문서 제목 관련 메모**: 파일명은 "v1"이지만, 표지(1페이지)에 표시된 실제 버전은 **v13**
> ("공식 최종판 · 더보기·부분에러 추가")이며 작성일 2026.09.13, "원본 시안 기준 확정"으로
> 표기됨. 프로젝트 커밋 로그에서도 "디자인 가이드 v13"이 여러 차례 언급된 바 있어, 동일
> 문서로 판단됨(파일명의 "v1"은 이 PDF 배포본 자체의 버전 번호로 추정, 가이드 내용 버전인
> v13과는 다른 번호 체계로 보임).

---

## 페이지 1 — 표지

**상태: 텍스트 확인 완료 · 표 없음 · 이미지 확인 완료(장식 배경) · 판독 불가 항목 없음**

어두운 남보라색 그라디언트 배경의 표지. 표/캡처 이미지 없음, 순수 텍스트.

| ID | 내용 |
|---|---|
| V1-P01-001 | 상단 라벨: "FAN:GO · OFFICIAL DESIGN SYSTEM" (영문 대문자, 레터스페이싱 넓음) |
| V1-P01-002 | 문서 제목: **"공식 디자인 가이드"** (흰색, 굵게, 대형) |
| V1-P01-003 | 부제 본문: "색상·서체·간격 같은 기초 토큰부터 헤더·버튼·입력필드·팝업·리스트·내비게이션까지, 모든 화면이 예외 없이 따라야 할 단 하나의 기준입니다. 좌측 상단 **"최종 수정안"**에서 지금 반영이 필요한 항목만 별도로 확인할 수 있습니다." |
| V1-P01-004 | 메타 정보 4항목(구분선 아래, 좌→우): **버전 "v13"**(부연: "공식 최종판 · 더보기·부분에러 추가") / **작성일 "2026.09.13"** / **"원본 시안"**(부연: "기준 확정") / **범위 "토큰 6 · 컴포넌트 15"** |

**코드 반영 필요 항목**: 없음(표지는 메타 정보뿐).
**판독 불가**: 없음.

---

## 페이지 2 — 최종 수정안 개요 + 대표 수정 1

**상태: 텍스트 확인 완료 · 표 없음 · Before/After 이미지 확인 완료 · 판독 불가 항목 없음**

| ID | 내용 |
|---|---|
| V1-P02-001 | 안내 카드 2개: (좌, 노란 배경) "지금 화면을 고치고 있다면 → 🚩 최종 수정안부터 보기 — 실제 화면 캡처와 수정 방향을 나란히 비교, 바로 작업 가능" / (우, 보라 배경) "새 화면을 새로 만든다면 → 📘 가이드 기준부터 보기 — 색상·타이포·컴포넌트 표준을 처음부터 순서대로 확인" |
| V1-P02-002 | 섹션 라벨 "— REVISION PLAN", 제목 "최종 수정안". 설명: "텍스트 설명은 최소화하고, 실제 화면(Before) → 수정 방향(After)을 나란히 두어 바로 작업할 수 있게 정리했습니다. 영향이 크고 실제 캡처 근거가 있는 항목은 **대표 수정**으로 큰 카드로, 나머지는 **그 외 수정**에 표로 압축했습니다." |
| V1-P02-003 | 소제목: "대표 수정 (Before → After)" |
| **V1-P02-004** | **대표 수정 #1 — "헤더 좌/우 아이콘 모양"**, 태그 "모양 수정". BEFORE: 보라색 헤더 바 안에 둥근 사각(squircle) 아이콘 버튼(흰색 ← 화살표), 캡션 "실제 시안 확인 결과 라운드 사각(squircle)". AFTER(가이드 반영): 보라색 헤더 바 좌상단에 훨씬 작은 "‹" 화살표만 표시, 캡션 **"radius 12px로 통일 · 05 헤더 규격표 반영 완료"** |

**코드 반영이 필요한 항목**: `AppHeader.jsx`/`AppHeader.module.css`의 `.top-bar-btn` 모양·radius가 12px squircle 기준에 맞는지 확인 필요(가이드 원문 캡션에 "05 헤더 규격표 반영 완료"라고 되어 있어, 가이드 문서 내에서는 이미 확정된 것으로 표기됨 — 실제 코드 대조는 이번 조사 범위 밖).
**판독 불가**: 없음.

---

## 페이지 3 — 대표 수정 2, 3

**상태: 텍스트 확인 완료 · 표 없음 · Before/After 캡처 이미지 확인 완료 · 판독 불가 항목 없음**

| ID | 내용 |
|---|---|
| **V1-P03-001** | **대표 수정 #2 — "프로필 드롭다운 아이콘 색상"**, 태그 "색상 통일 필요". BEFORE: 실제 앱 캡처(로그인 사용자 "test01" / test01@gmail.com), 메뉴 항목 각각 **컬러 아이콘** 사용 — 마이페이지(보라 톤 사람 아이콘), 환경설정(회색 톱니바퀴), 여행 히스토리(파란 비행기), 로그아웃(주황/갈색 도어 아이콘). 캡션: **"컬러 아이콘(사람·톱니바퀴·비행기·도어) 사용 중"**. AFTER(반영 필요 — 아직 미반영 상태로 표기됨): 마이페이지/환경설정/로그아웃 3항목만 **선형 단색 아이콘**(사람 윤곽, 점선 원(설정 느낌), 화살표-박스(로그아웃))으로 교체. 캡션: **"05 아이콘 규칙(선형·단색)에 맞춰 전면 교체"** |
| **V1-P03-002** | **대표 수정 #3 — "이벤트 리스트 "고정" 강조"**, 태그 "강조 방식 변경". BEFORE: 리스트 행 배경 전체를 Primary(보라)로 채움, 흰 글자, 우측 "고정 ✓" 표시. 캡션: **"행 배경 전체를 Primary로 채움"**. AFTER: 배경은 흰색, **좌측 세로 액센트 바**(보라) + 우측 알약형 "고정 ✓" 배지만 유지. 캡션: **"좌측 액센트 바 + "고정 ✓" 배지만 유지, 배경은 흰색"** |

**코드 반영이 필요한 항목**:
- 프로필 드롭다운(AppHeader.jsx `menu-item`들의 아이콘) — 현재 코드는 이미 `Icon.jsx`의 선형 단색 아이콘(`person`/`gear`/`plane`/`logout`)을 쓰고 있어 **가이드의 AFTER 상태와 일치하는 것으로 보임**(재확인 권장, 이번 조사에서는 PDF 내용만 기록).
- 이벤트 리스트 "고정" 강조 — 좌측 액센트 바 + 배지 방식은 프로젝트 커밋 이력상 이미 유사하게 반영된 것으로 보이나(`EventSelectView.module.css` `.pinned` 관련), 페이지 안 비교는 이번 조사 범위 밖.

**판독 불가**: 없음.

---

## 페이지 4 — 대표 수정 4

**상태: 텍스트 확인 완료 · 표 없음 · Before/After 캡처 이미지 확인 완료 · 판독 불가 항목 없음**

| ID | 내용 |
|---|---|
| **V1-P04-001** | **대표 수정 #4 — "정보 확인 팝업의 중복 닫기"**, 태그 "로직 수정". BEFORE: 지도 이미지 placeholder("MAP IMAGE"), "사진 출처: Google Map, Kakao Map" 표기, "MAP" 라벨, 주소 "서울 송파구 올림픽로 424", 제목 "[엔시티 127(NCT 127)] NCT 127 5TH TOUR 'NEO CITY : SEOUL – THE REDLINE' – 서울(9/18)", "09.18 입장 / 시작 19:00", 주소+"콘서트" 재표기, 하단 전체 폭 보라색 **"확인"** 버튼 + 우상단 **X** 버튼(둘 다 있음). 캡션: **"X와 "확인"이 같은 동작(닫기)을 중복 수행"**. AFTER: 지도 이미지 제거, 카드에 제목 "NCT 127 5TH TOUR", "09.18 입장 / 시작 19:00"만 남기고 **X 버튼만 유지**, 하단 "확인" 버튼 삭제. 캡션: **"하단 "확인" 버튼 삭제, X만 유지"** |

**코드 반영이 필요한 항목**: 이 항목은 프로젝트에서 이미 별도로 처리된 것으로 보임 — 세션 작업 이력상 `EventSelectView.jsx`의 "정보확인 팝업"(`EventDetailModal`)을 **완전히 삭제**하고 리스트에서 바로 선택하는 방식으로 이미 단순화했음(사용자 명시적 요청: "팝업에서 포스터 사진 보여주려고 했는데 매번 바뀔때마다 사진을 바꿀수 없음"). 즉 가이드는 "팝업 개선(X만 유지)" 방향이지만, 실제 프로젝트는 "팝업 자체를 제거"하는 더 강한 방향으로 이미 진행됨 — **가이드와 실제 구현 방향이 다르므로 팀 확인 필요**.

**판독 불가**: 없음.

---

## 페이지 5 — 대표 수정 5

**상태: 텍스트 확인 완료 · 표 없음 · Before/After 캡처 이미지 확인 완료 · 판독 불가 항목 없음**

| ID | 내용 |
|---|---|
| **V1-P05-001** | **대표 수정 #5 — "약관 모달의 중복 닫기"**, 태그 "로직 수정". BEFORE: "이용약관" 모달, 우상단 **X**, 본문(제1장 총칙, 제1조~제4조 조항 전문 스크롤 가능), 하단에 **"닫기"**(아웃라인)와 **"동의하고 닫기"**(보라 채움) 버튼까지 총 3가지 닫는 방법. 캡션: **"X·닫기·동의하고 닫기 — 닫는 방법이 3가지"**. AFTER: X 제거, 본문은 "제1장 총칙 ..."으로 요약 표시, **"닫기"/"동의하고 닫기"** 버튼 2개만 유지. 캡션: **"X 삭제, 결정 버튼 2개만 유지 — 명확한 선택을 강제"** |

**코드 반영이 필요한 항목**: `AgreementModal.jsx` 확인 필요 — X 버튼 존재 여부, 닫기/동의 버튼 2개 구조인지 대조 필요(이번 조사에서는 PDF 내용만 기록, 코드 대조는 별도 작업으로 남김).

**판독 불가**: 없음.

---

## 페이지 6 — 대표 수정 6

**상태: 텍스트 확인 완료 · 표 없음 · Before/After 캡처 이미지 확인 완료 · 판독 불가 항목 없음**

| ID | 내용 |
|---|---|
| **V1-P06-001** | **대표 수정 #6 — "헤더 우측 "사람" 아이콘"**, 태그 "개념 수정". BEFORE: 보라 헤더 우측에 사람(프로필) 아이콘. 캡션: **"사람(프로필) 아이콘 — "마이페이지 바로가기"로 오해됨"**. AFTER: **햄버거 메뉴 아이콘(≡)**으로 교체. 캡션: **"메뉴 아이콘(≡)으로 교체 — 마이페이지는 그 안의 항목 중 하나"** |

**코드 반영이 필요한 항목**: 현재 `AppHeader.jsx`는 이미 `Icon name="menu"`(≡)를 헤더 우측에 쓰고 있어 **가이드의 AFTER 상태와 일치**하는 것으로 보임(추가 조치 불필요, 재확인만 권장).

**판독 불가**: 없음.

---

## 페이지 7 — 대표 수정 7, 8

**상태: 텍스트 확인 완료 · 표 없음 · Before/After 캡처 이미지 확인 완료 · 판독 불가 항목 없음**

| ID | 내용 |
|---|---|
| **V1-P07-001** | **대표 수정 #7 — "에러 화면 배경"**, 태그 "규칙 위반". BEFORE: 화면 전체가 Primary(보라) 배경, 흰 텍스트로 "이동시간 조회에 실패했습니다: KAKAO_REST_API_KEY가 .env에 설정되어 있지 않습니다. (필드: )", 버튼 "확인 화면으로"(연보라 알약)와 "다시 시도"(흰색 알약). 캡션: **"Primary 전면 채움 — 스플래시 전용 예외를 위반"**. AFTER: 흰 배경 카드, 중앙 정렬 주황색 경고(⚠) 아이콘 + 굵은 텍스트 **"이동 시간을 불러오지 못했어요"**. 캡션: **"흰 배경 + 중앙 정렬 아이콘·문구로 전환"** |
| **V1-P07-002** | **대표 수정 #8 — "에러 문구 원문 노출"**, 태그 "보안·문안 수정". BEFORE: 보라 박스 안 흰 글씨로 **"이동시간 조회에 실패했습니다: KAKAO_REST_API_KEY가 .env에 설정되어 있지 않습니다."**(서버 환경변수명 그대로 노출). 캡션: **"서버 환경변수 원문이 그대로 노출됨"**. AFTER: 흰 박스, 굵게 **"이동 시간을 불러오지 못했어요"** + 작은 회색 글씨 **"잠시 후 다시 시도해주세요"**. 캡션: **"사람이 이해할 문장으로 치환, 개발정보 절대 노출 금지"** |

**코드 반영이 필요한 항목**: 세션 작업 이력상 `api.js`의 `safeErrorMessage`/`safeText` 유틸이 정확히 이 규칙("백엔드 원문 노출 금지, 사람이 이해할 문구로 치환")을 구현하기 위해 만들어졌고 여러 화면에 이미 적용됨 — **가이드와 실제 구현 방향이 일치**하는 것으로 보임. 다만 "에러 화면 배경 흰색" 규칙이 **모든** 에러 상태(비단 이동시간 조회뿐 아니라)에 일관되게 적용됐는지는 전수 확인 필요.

**판독 불가**: 없음.

---

## 페이지 8 — 대표 수정 9 ⚠️ 최근 작업과 상충 가능성

**상태: 텍스트 확인 완료 · 표 없음 · Before/After 캡처 이미지 확인 완료 · 판독 불가 항목 없음**

| ID | 내용 |
|---|---|
| **V1-P08-001** | **대표 수정 #9 — "복잡한 버튼 배치 단순화"**, 태그 "배치 수정". BEFORE: 실제 앱 캡처("SC-02 · 메인 홈 - 지도/타임라인 전환(동작함)" 표기). 화면 상단 메모: "1일 2일 3일 각자의 동선 보기 가능". 캡처 내용: "여행 일정 9.13 — 9.15" 타이틀, 지도 일러스트, "MAP 총 5개 장소", 장소 리스트 4개(하이브 인사이트 94점, 낙산공원 88점, 어니언 안국 71점, 광야@서울 71점), 하단에 **버튼 3개가 한 줄**: **"목록 열기" / "동선 수정" / "다음: 확정 →"**, 그 아래 4탭 하단바. 별도 메모: "숙소 선택시 → 지도 검색 + 체크인 체크아웃 기간 선택". 캡션: **"하단에 버튼 3개("목록 열기"·"동선 수정"·"다음: 확정 →")가 한 줄에 몰려 어떤 게 주요 행동인지 알기 어려움"**. AFTER(가이드 확정안, 태그 "AFTER · 확정"): "2박 · 하이엔 인사이트" 캡션 텍스트, 전체 폭 보라 버튼 **"숙소로 추가"**, 그 아래 2개 버튼 **"동선 수정"**(아웃라인) / **"확인"**(보라 채움). 캡션: **""목록 열기"·"다음"(확인과 동작 중복) 모두 제거. 06 버튼 섹션 확정안과 동일"** |

**⚠️ 코드 반영 시 반드시 확인 필요한 충돌 항목**:
이 가이드 규칙은 **"버튼 3개 → 2개(동선 수정/확인), "목록 열기" 완전 제거"**를 명시적으로 확정(AFTER·확정)하고 있음. 실제 프로젝트 커밋 이력을 보면:
1. 과거 한 시점에 이미 이 가이드대로 3개→2개로 줄인 이력이 있음(커밋 메시지: "동선 확인 화면 하단 버튼 3개(목록 열기/동선 수정/다음:확정) → 2개(동선 수정/확인)").
2. 그런데 **이번 세션에서 사용자가 명시적으로 다시 3번째 버튼("동선보기", 지도 화면 연결)을 요청**해서, `ScheduleTableView.jsx`/`ItineraryView.jsx`에 버튼을 3개로 **다시 늘린 상태**임(현재 코드 상태: "동선보기" / "동선 수정" / "확인" 3버튼).

→ 즉 **PDF 가이드(v13, 확정)와 실제 사용자의 최근 명시적 요구사항이 정면으로 배치됨.** 이 문서는 내용만 기록하고 코드는 건드리지 않았으므로, 이 충돌은 **사람이 직접 판단해서 정리해야 할 항목**으로 남겨둠(가이드를 업데이트할지, 화면을 가이드에 맞춰 되돌릴지 결정 필요).

**판독 불가**: 없음.

---

## 페이지 9 — 대표 수정 10 ⚠️ 삭제된 화면 관련

**상태: 텍스트 확인 완료 · 표 없음 · Before/After 캡처 이미지 확인 완료 · 판독 불가 항목 없음**

| ID | 내용 |
|---|---|
| **V1-P09-001** | **대표 수정 #10 — ""저장한 동선" 목록에 리스트 미제한·중복 노출"**, 태그 "데이터·리스트 수정". BEFORE: 실제 앱 캡처, 헤더(← / FAN:GO 로고 / 사람 아이콘 — 주의: 여기서도 구버전 "사람" 아이콘이 캡처됨), 타이틀 **"저장한 동선"**, 동일한 항목 **"[엔시티 드림(NCT DREAM)] 동국대학교 2026 영캠프 Youngcamp (9/17)"**(우측 "예정" 배지, "26.09.16 — 26.09.18 · 0곳")이 **7개 이상 반복**, 챗봇 FAB 아이콘이 리스트 위에 겹쳐 보임, "더보기"/리스트 끝 표시 없이 무한 스크롤. 캡션: **"같은 항목이 7개 이상 반복 노출되고, 리스트 끝(더보기)이 없어 스크롤이 끝없이 이어짐"**. AFTER: 단일 카드 **"NCT DREAM 영캠프 Youngcamp"**, "09.16 — 09.18 · 0곳", "예정" 배지, 그 아래 **"더보기 ▾"** 아웃라인 버튼. 캡션: **"① 동일 데이터 중복 노출 여부 서버 데이터 점검 필요 ② 08 리스트 섹션의 "더보기" 규칙(8~10개 단위) 적용"** |

**⚠️ 코드 반영 시 반드시 확인 필요한 충돌 항목**:
이 가이드 항목이 캡처한 화면은 **"저장한 동선"**이라는 타이틀을 가진 화면으로, 프로젝트의 `MyTripView.jsx`(경로 `/trip/my`)에 해당하는 것으로 판단됨. 그런데 **이번 세션에서 사용자가 "[저장한 동선] 화면은 아예 삭제해줘"라고 명시적으로 지시**하여, 해당 화면 컴포넌트(`MyTripView.jsx`/`.module.css`)와 라우트를 **완전히 삭제**했고, 하단 내비의 "일정" 탭과 동선 확정 후 이동 경로를 전부 **"나의 일정"(`HistoryView.jsx`, `/trip/history`) 화면으로 통합**한 상태임(사유: "나의 일정"과 화면이 중복된다는 사용자 판단).

→ 즉 이 가이드 항목이 다루는 화면 자체가 **현재 코드베이스에는 더 이상 존재하지 않음**. 가이드의 "더보기 8~10개 단위" 리스트 규칙은 필요하다면 **살아남은 "나의 일정"(HistoryView) 화면**에 적용하는 것으로 재해석해야 할 가능성이 있음 — 실제로 `HistoryView.jsx`에는 이미 "8개 단위 더보기" 페이지네이션이 구현되어 있는 것으로 보임(세션 메모리 기준, 재확인 필요).

**판독 불가**: 없음.

---

## 페이지 10 — "그 외 수정" 요약표 + 최종 체크리스트(도입부)

**상태: 텍스트 확인 완료 · 표 확인 완료(10행) · 이미지 없음(순수 표+텍스트) · 판독 불가 항목 없음**

도입 문구: "아래는 기준이 이미 Foundations·Components 섹션에 확정되어 있어 별도 큰 카드 없이 표로 압축한 항목입니다. 화면 작업 시 해당 섹션을 펼쳐 그대로 적용하면 됩니다."

### 표: "그 외 수정 (요약)" — 열: 항목 / 이전 상태 / 적용할 기준

| ID | 항목 | 이전 상태 | 적용할 기준 |
|---|---|---|---|
| V1-P10-001 | 뒤로가기 버튼 스타일 | 원형 배경 유/무 혼재 | 05 헤더 — squircle 12px 통일 |
| V1-P10-002 | 상태바 아이콘 색 | 화면마다 표기 방식 다름 | 03 여백 — 헤더가 Primary이므로 흰색 고정 |
| V1-P10-003 | 카드 여백·그림자 | 12~24px 제각각 | 04 라운드·그림자 — 8px 그리드 고정값 |
| V1-P10-004 | 아이콘 세트 | 선형/면 혼용 | 05 아이콘 — 선형 단색 1종 |
| V1-P10-005 | 화면 타이틀 정렬 | 좌측/중앙 혼용 | 02 타이포 — 화면 대제목은 좌측 정렬 고정 |
| V1-P10-006 | 점수·퍼센트 배지 색 | 파랑·주황·초록 기준 없이 혼재 | 01 컬러 — 80~100 초록 / 50~79 주황 / 0~49 빨강 |
| V1-P10-007 | 진행 상태 표기 | 배지형/텍스트형 혼용 | 07 배지 — 알약형 배지로 통일 |
| V1-P10-008 | 입력폼 상태 | 테두리·라벨 스타일 제각각 | 06 입력 필드 — 기본/포커스/에러/비활성 4상태 고정 |
| V1-P10-009 | 하단 내비게이션 | 3탭/5탭 혼용 | 08 하단 내비 — 홈·일정·채팅·마이 4탭 고정 |
| V1-P10-010 | 버튼 화살표 전체 | 일부 버튼에만 화살표 | 06 버튼 — 전 버튼 화살표 금지 |

**⚠️ 참고**: 이 표는 "01 컬러", "02 타이포", "03 여백", "04 라운드·그림자", "05 헤더/아이콘", "06 버튼/입력필드", "07 배지", "08 하단 내비" 같은 **번호 붙은 기준 섹션을 인용만 할 뿐, 그 섹션들의 실제 수치(hex 코드, px 값 등)는 pages 1~10 안에는 나오지 않음**. 즉 **1~10페이지는 "최종 수정안"(Before/After 비교) 파트이고, 색상 hex·폰트 크기·spacing 수치 등 실제 토큰 정의는 11페이지 이후("가이드 기준"/Foundations 섹션)에 있는 것으로 추정됨** — 이번 조사 범위 밖이라 미확인.

**코드 반영이 필요한 항목**:
- V1-P10-006(점수·퍼센트 배지 색 80/50 기준 초록·주황·빨강)은 `src/scoreColor.js`에 이미 동일 로직(`80~100 Success, 50~79 Warning, 0~49 Danger`)으로 구현되어 있어 **가이드와 일치**.
- V1-P10-009(하단 내비 4탭: 홈·일정·채팅·마이)는 `BottomNav.jsx`의 `TABS` 배열과 **일치**(홈/일정/채팅/마이 4개).
- 나머지(V1-P10-001~005, 007~008, 010)는 코드 대조 전수 확인이 이번 조사 범위 밖.

### 최종 체크리스트 (페이지 10 하단 — 도입부만 보이고 항목 계속됨)

제목: **"최종 체크리스트"**. 체크박스(☐) 형태로 이어지는 목록으로, 페이지 10에서 보이는 첫 8개 항목만 기록(9번째 항목부터는 페이지 잘림으로 11페이지에 이어지는 것으로 추정 — **11페이지 이후이므로 이번 조사에서는 기록하지 않음**):

| ID | 체크리스트 항목 |
|---|---|
| V1-P10-011 | ☐ 헤더 좌/우 아이콘 원형 → 라운드 사각(12px) |
| V1-P10-012 | ☐ 드롭다운 메뉴 아이콘 컬러 → 선형 단색 |
| V1-P10-013 | ☐ 이벤트 리스트 강조를 전체 채움 → 좌측 액센트 바 |
| V1-P10-014 | ☐ 정보 확인 팝업의 "확인" 버튼 삭제 (X만 유지) |
| V1-P10-015 | ☐ 약관 모달의 X 삭제 (결정 버튼 2개만 유지) |
| V1-P10-016 | ☐ 모든 버튼 화살표 제거, 표준 버튼명으로 교체 |
| V1-P10-017 | ☐ 헤더 우측 사람 아이콘 → 메뉴 아이콘(≡)으로 교체 |
| V1-P10-018 | ☐ 에러 화면 배경을 Primary → 흰색으로 전환 |

(페이지 10은 이 8번째 항목 아래로 줄이 하나 더 있으나 텍스트가 페이지 경계에 걸려 다음 페이지로 이어짐 — **여기서 멈춤, 11페이지 이후 미분석**)

**판독 불가**: 없음(모든 텍스트·표 항목이 선명하게 렌더링됨).

---

## 종합 요약

### Do / Don't 패턴 (1~10페이지에서 반복적으로 나타난 원칙)

| ID | Do | Don't |
|---|---|---|
| V1-SUM-001 | 팝업/모달의 닫기 방법은 **하나의 의미 단위**로 통일(예: X 하나만, 또는 결정 버튼 2개만) | 같은 "닫기" 동작을 X·버튼 등 **여러 방법으로 중복 제공하지 않기** |
| V1-SUM-002 | 하단 고정 CTA는 **주된 행동 1~2개**로 압축 | 버튼을 **3개 이상 한 줄에 몰아넣지 않기**(우선순위가 안 보임) |
| V1-SUM-003 | 아이콘은 **선형·단색 1종**으로 통일 | 컬러 아이콘·면(filled) 아이콘 **혼용 금지** |
| V1-SUM-004 | 에러 화면은 **흰 배경 + 중앙 정렬 아이콘/문구** | Primary 전면 채움은 **스플래시 전용**, 에러 화면에 쓰지 않기 |
| V1-SUM-005 | 에러 문구는 **사람이 이해할 문장**으로 가공 | 서버 환경변수명·API 키 등 **개발 정보 원문 노출 금지** |
| V1-SUM-006 | 강조가 필요한 리스트 행은 **좌측 액센트 바 + 배지** | 행 **배경 전체를 Primary로 채우지 않기** |
| V1-SUM-007 | 리스트는 **"더보기" 8~10개 단위**로 구간을 끊기 | 리스트를 **끝없이(무한 스크롤) 반복 노출하지 않기** |
| V1-SUM-008 | 헤더 우측은 **메뉴(≡) 아이콘**으로 통일 | **사람 아이콘**을 "마이페이지 바로가기"로 오해되게 두지 않기 |

### 코드 반영이 필요한 항목 전체 목록 (요약)

1. **V1-P02-004** — 헤더 좌/우 버튼: squircle → radius 12px 사각으로 통일됐는지 재확인
2. **V1-P03-001** — 프로필 드롭다운 아이콘: 컬러 → 선형 단색 (현재 코드 일치 추정, 재확인 권장)
3. **V1-P03-002** — 이벤트 리스트 "고정" 강조: 전체 채움 → 좌측 액센트 바+배지 (현재 코드 일치 추정)
4. **V1-P04-001** — 정보 확인 팝업: 가이드는 "X만 유지"지만 실제로는 **팝업 자체를 삭제**함(더 강한 조치) — 방향 재확인 필요
5. **V1-P05-001** — 약관 모달(`AgreementModal.jsx`): X 삭제 여부 코드 대조 필요
6. **V1-P06-001** — 헤더 우측 아이콘: 메뉴(≡) 적용 이미 일치 추정
7. **V1-P07-001/002** — 에러 화면 흰 배경 + 안전한 문구(`safeErrorMessage`/`safeText`) 이미 일치 추정, 전수 적용 여부만 재확인
8. **V1-P08-001** — ⚠️ **버튼 3개→2개 규칙과 이번 세션의 "동선보기" 버튼 재추가 요청이 상충** — 사람 판단 필요
9. **V1-P09-001** — ⚠️ **"저장한 동선" 화면(MyTripView) 자체가 이번 세션에서 삭제됨** — 가이드 항목 적용 대상 화면이 사라짐, "나의 일정"(HistoryView)으로 대체 여부 확인 필요
10. **V1-P10-001~010** — "그 외 수정" 10개 항목: 점수 배지색(V1-P10-006)·하단 내비 4탭(V1-P10-009)은 코드와 일치 확인됨, 나머지는 미확인

### 페이지별 상태 총괄

| 페이지 | 텍스트 | 표 | 이미지 | 판독 불가 |
|---|---|---|---|---|
| 1 | 확인 완료 | 해당 없음 | 확인 완료(배경만) | 없음 |
| 2 | 확인 완료 | 해당 없음 | 확인 완료 | 없음 |
| 3 | 확인 완료 | 해당 없음 | 확인 완료 | 없음 |
| 4 | 확인 완료 | 해당 없음 | 확인 완료 | 없음 |
| 5 | 확인 완료 | 해당 없음 | 확인 완료 | 없음 |
| 6 | 확인 완료 | 해당 없음 | 확인 완료 | 없음 |
| 7 | 확인 완료 | 해당 없음 | 확인 완료 | 없음 |
| 8 | 확인 완료 | 해당 없음 | 확인 완료 | 없음 |
| 9 | 확인 완료 | 해당 없음 | 확인 완료 | 없음 |
| 10 | 확인 완료 | 확인 완료 | 해당 없음(순수 표) | 없음 |

**전체 결론**: 1~10페이지는 렌더링 화질(2.5배율)에서 모든 텍스트·캡처 이미지·표가 선명하게 판독됨 — 판독 불가 항목 없음. 다만 **색상 hex·폰트 px·spacing 수치 등 구체적 토큰 값은 이 10페이지 안에 없고**, 번호(01~08)로 참조만 되고 있어 **11페이지 이후에 있을 것으로 추정**(미분석). 또한 **2건의 중대한 충돌**(V1-P08-001, V1-P09-001)이 발견되어, 코드에 아무 반영도 하지 않은 채 이 문서에 기록만 해 둠 — 다음 단계 진행 전 사람의 판단이 필요함.
