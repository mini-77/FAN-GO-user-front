# FAN:GO 공통 디자인 가이드 — 11~25페이지 분석

- 원본 파일: `260913_FANGO_공통_디자인_가이드_v1 1.pdf` (사용자 Downloads 폴더, "이전에 전달한 v1 PDF")
- 총 페이지: 43p (이 문서는 그중 **11~25페이지**만 다룸)
- 확인 방법: PyMuPDF로 각 페이지를 2.5배율 PNG로 렌더링한 뒤 이미지를 직접 육안으로 확인함
- 코드/디자인 수정 없음 — 이 문서는 내용 기록 전용
- 기존 파일(`00_project_structure.md`, `01_v1_pages_01_10.md`, `03_v1_pages_26_43.md`)은 열람만 하고 **삭제·덮어쓰기하지 않음**

**⚠️ 이 구간이 가장 중요한 이유**: 11~25페이지는 문서의 **"FOUNDATIONS"(토큰 원본)**와 **"COMPONENTS"(헤더·버튼·배지)** 앞부분을 담고 있어, 앞서 26~43페이지 분석에서 "값을 추정만 하고 원본을 못 봤다"고 표시했던 색상 hex·폰트·spacing·헤더 규격 수치의 **원본 정의**가 대부분 여기 있음. 또한 `03_v1_pages_26_43.md`에서 발견했던 "다음: 버튼" 관련 내부 충돌도 이 구간(23~24p)을 읽고 나니 **더 명확한 그림**이 나옴(하단 "종합 요약" 참고).

---

## 페이지 11 — 체크리스트 마무리 + 개요&원칙(4대 원칙)

**상태: 텍스트 확인 완료 · 표 없음 · 이미지 없음 · 판독 불가 항목 없음**

상단은 10p에서 이어지는 "최종 체크리스트"의 나머지 항목(체크박스).

| ID | 내용 |
|---|---|
| V1-P11-001 | ☐ 에러 문구에서 API 키·환경변수 등 개발정보 노출 제거, 사용자 문장으로 교체 |
| **V1-P11-002** | ☐ 하단 버튼을 **"동선 수정·확인" 2개로 정리** ("목록 열기"·중복된 "다음" 버튼 제거) |
| V1-P11-003 | ☐ 길이 제한 없는 리스트(이벤트·저장한 동선 등)에 "더보기" 적용, 무한스크롤 금지 |
| V1-P11-004 | ☐ "저장한 동선" 목록 중복 데이터 원인 서버 점검 |
| V1-P11-005 | 새 섹션 "— OVERVIEW" → "개요 & 원칙": "이 문서는 진단·수정 이력을 다루지 않는 **순수 기준 문서**입니다. 화면별 문제점은 별도의 "수정사항 리포트"를 참고하세요." |

### 4대 원칙 (2×2 카드)

| ID | 원칙 | 설명 |
|---|---|---|
| V1-P11-006 | 01 일관성 | 같은 역할의 요소는 화면이 달라도 항상 같은 모양·색·크기를 가진다. |
| V1-P11-007 | 02 위계 | 중요한 것만 강조한다. 강조 색과 굵기는 화면당 핵심 하나에만 쓴다. |
| V1-P11-008 | 03 절제 | 중심색은 하나. 배경을 전면으로 채우지 않고 포인트로만 사용한다. |
| **V1-P11-009** | 04 명료함 | 버튼과 문구는 짧고 예측 가능하게. **화살표·아이콘 단독 버튼을 두지 않는다.** |

**참고**: V1-P11-009는 "화살표·아이콘 **단독** 버튼"(텍스트 라벨 없이 화살표나 아이콘만 있는 버튼)을 금지하는 것으로 읽히며, 텍스트+화살표 조합 자체를 금지하는 문장은 아님 — 화살표 관련 상세 규칙은 페이지 23~24("06 버튼")에 별도로 있음(아래 참고).

**판독 불가**: 없음.

---

## 페이지 12 — 컬러 (Primary Scale + Semantic 색상 4종)

**상태: 텍스트 확인 완료 · 표 없음(카드형) · 색상 스와치 이미지 확인 완료 · 판독 불가 항목 없음**

섹션 "— FOUNDATIONS", 제목 **"컬러"**.

| ID | 내용 |
|---|---|
| V1-P12-001 | 설명: "Primary 한 가지 색을 중심으로, 짙기 **8단계(Scale)**와 의미별 보조색 4가지, 텍스트/배경용 뉴트럴 톤으로 구성합니다." |
| V1-P12-002 | **"Primary Scale"**: 8단 스와치 바 — **50 / 100 / 200 / 400 / 500·Base / 600 / 700 / 900** (300·800 단계는 없음). 스와치에는 진하기만 표시되고 이 페이지에는 hex 코드가 적혀있지 않음(육안 판독 불가 — 정확한 hex는 별도 확인 필요, 코드의 `index.css` `--color-primary-*` 값과 대조 권장) |

### "Semantic & Neutral" 카드 6개 (색상 스와치 + 이름 + hex + 용도)

| ID | 이름 | hex | 용도 |
|---|---|---|---|
| **V1-P12-003** | Success | **#2E9E5B** | 완료·여유(80~100점) |
| **V1-P12-004** | Warning | **#E5A02E** | 보통(50~79점)·시간 고정 |
| **V1-P12-005** | Danger | **#E5484D** | 삭제·혼잡(0~49점)·에러 |
| **V1-P12-006** | Info | **#2E7CE9** | 안내 배너·링크 |
| **V1-P12-007** | Ink 900 | **#17161F** | 제목·본문 텍스트 |
| **V1-P12-008** | Ink 600 | **#5C5D6E** | 보조 텍스트 |

**코드 반영 확인**: `src/index.css`의 `--color-success/#2E9E5B`, `--color-warning/#E5A02E`, `--color-danger/#E5484D`, `--color-info/#2E7CE9`, `--color-ink-900/#17161F`, `--color-ink-600/#5C5D6E`가 **전부 정확히 일치**함. Primary Scale의 hex는 이 페이지에서 판독 불가하여 대조하지 못함(아래 "판독 불가" 참고).

**판독 불가**: Primary Scale 8단계(50~900)의 정확한 hex 코드 — 스와치 이미지의 색만 보이고 텍스트로 된 hex 값이 이 페이지에는 없음(13p에서 이어지는 Neutral 카드들과 달리 Primary Scale 카드에는 hex 라벨이 표시되지 않음). 실제 렌더링 색상을 픽셀 단위로 추출하는 방식이 아니라 육안 판독이므로, 정확한 hex는 코드(`index.css`의 `--color-primary-*`)를 기준으로 삼는 것을 권장.

---

## 페이지 13 — 컬러 (Neutral 2종 + 사용 규칙표)

**상태: 텍스트 확인 완료 · 표 확인 완료(5행) · 색상 스와치 이미지 확인 완료 · 판독 불가 항목 없음**

| ID | 이름 | hex | 용도 |
|---|---|---|---|
| **V1-P13-001** | Ink 400 | **#93949F** | placeholder·비활성 |
| **V1-P13-002** | Ink 200 | **#E2E2E9** | 구분선·비활성 배경 |

### 표: 속성 / 규칙

| ID | 속성 | 규칙 |
|---|---|---|
| **V1-P13-003** | 배경(Surface) | 화면 기본 배경은 항상 **#FFFFFF**. Primary 전면 채움은 스플래시 1개 화면에만 허용 |
| **V1-P13-004** | 강조 비중 | 한 화면 안에서 **Primary 사용 영역은 전체의 15%를 넘지 않는다** |
| V1-P13-005 | 텍스트 대비 | Primary 배경 위 텍스트는 흰색만 사용 (대비 4.5:1 이상) |
| V1-P13-006 | 점수·상태 색 | 80~100 Success · 50~79 Warning · 0~49 Danger 고정 |
| **V1-P13-007** | 다크 시트(피커) | 날짜·시간 선택 등 시스템 피커류는 다크 배경(**#2B2A3A**) 예외 허용 |

**코드 반영 확인**: `--color-ink-400/#93949F`, `--color-ink-200/#E2E2E9` **일치**. `PickerSheet.module.css`의 `#2B2A3A` **일치**(재확인). V1-P13-004("Primary 사용 영역 15% 이하")는 정량적 기준이라 코드로 직접 검증하기 어려우나, 각 화면의 Primary 색 사용 범위(헤더 바, 강조 배지, 주요 버튼 등)가 전체 화면 대비 과도하지 않은지 육안 점검 권장.

**판독 불가**: 없음.

---

## 페이지 14 — 타이포그래피 (서체·기본 규칙) ⚠️ 실제 코드와 폰트 자체가 다름

**상태: 텍스트 확인 완료 · 표 확인 완료(6행) · 타입 스케일 예시 4개 확인 완료 · 판독 불가 항목 없음**

섹션 "— FOUNDATIONS", 제목 **"타이포그래피"**.

| ID | 내용 |
|---|---|
| **V1-P14-001** | 설명: "서체는 **맑은 고딕 한 종류**, 굵기는 **Regular(400)·Bold(700~800) 두 가지만** 사용합니다." |

### 표: 속성 / 값

| ID | 속성 | 값 |
|---|---|---|
| **V1-P14-002** | 기본 서체 | **맑은 고딕 (Malgun Gothic)** |
| **V1-P14-003** | 대체 서체 | **Apple SD Gothic Neo, sans-serif** |
| V1-P14-004 | 본문 줄간격 | 1.5 |
| V1-P14-005 | 제목 줄간격 / 자간 | 1.3 / -0.3px |
| **V1-P14-006** | 화면 대제목(H1) 정렬 | **항상 좌측 정렬** |
| V1-P14-007 | 팝업/모달 제목 정렬 | 중앙 정렬 (예외) |

### 타입 스케일 예시 (페이지 14~15에 걸쳐 나옴)

| ID | 예시 텍스트 | 역할 | 크기/굵기 |
|---|---|---|---|
| V1-P14-008 | "88" | Display | 28px / Bold |
| **V1-P14-009** | "여행 일정을 확인해요" | H1(화면 대제목) | **22px / Bold** |
| V1-P14-010 | "좋아하는 아티스트를 골라주세요" | H2(섹션 제목) | 18px / Bold |
| V1-P14-011 | "낙산공원" | Subtitle(카드·리스트 제목) | 15px / Bold |

**⚠️ 코드 반영 시 반드시 확인 필요한 충돌 — 폰트 패밀리 자체가 다름**:
가이드는 **"맑은 고딕(Malgun Gothic)"**을 기본 서체로, **"Apple SD Gothic Neo, sans-serif"**를 대체 서체로 명시하고 있음(둘 다 **시스템 폰트** — 별도로 다운로드하지 않아도 Windows/macOS에 이미 설치되어 있는 서체). 그런데 실제 코드베이스는 거의 모든 `*.module.css` 파일 상단에서 **Google Fonts**(`IBM Plex Sans KR`, `Urbanist`, `Inter`)를 `@import`해서 쓰고 있음 — **가이드가 지정한 서체와 완전히 다른 서체**가 적용되어 있음. 이건 단순 수치 차이가 아니라 **폰트 자체의 정체성이 다른 수준의 불일치**라서, 실수인지(가이드 작성 시점에는 시스템 폰트 기준이었다가 나중에 구글 폰트로 바뀐 것인지) 의도적 변경인지 팀 확인이 필요함.

V1-P14-006(H1 항상 좌측 정렬)은 앞서 `01_v1_pages_01_10.md`의 V1-P10-005("화면 타이틀 정렬: 좌측 정렬 고정")와 **일치**하며, 실제 대부분의 화면(TripDateView, EventSelectView 등)이 이미 좌측 정렬 타이틀을 쓰고 있어 **부합**하는 것으로 보임.

**판독 불가**: 없음.

---

## 페이지 15 — 타이포그래피 (Body/Caption/Micro) + 다중 선택 텍스트 위계(도입)

**상태: 텍스트 확인 완료 · 표 없음(카드형) · 이미지 없음 · 판독 불가 항목 없음**

| ID | 예시 텍스트 | 역할 | 크기/굵기 |
|---|---|---|---|
| V1-P15-001 | "하이브 인사이트 · 오전 09:00 – 오후 17:00" | Body | 14px / Regular |
| V1-P15-002 | "서울 · 5곳 · B안" | Caption | 12px / Medium |
| **V1-P15-003** | "반경 5km · 3곳" | Micro | **11px / 최소 크기** |

| ID | 내용 |
|---|---|
| V1-P15-004 | 소제목 "다중 선택 화면 텍스트 위계 — 확정 (우선순위·멤버 선택 공통 기준)": ""선호 액티비티", "멤버 선택" 같은 다중 선택 화면에 실제로 등장하는 문구 **11종**을 역할별로 고정합니다. 새 다중 선택 화면을 만들 때도 이 표만 따르면 됩니다." (표 본문은 다음 페이지) |

V1-P15-003(Micro 11px = 최소 크기)은 `01_v1_pages_01_10.md`의 V1-P42-003("최소 글자 크기 11px")과 **일치**.

**판독 불가**: 없음.

---

## 페이지 16 — 다중 선택 화면 텍스트 위계 (전체 표, 11행)

**상태: 텍스트 확인 완료 · 표 확인 완료(11행) · 이미지 없음 · 판독 불가 항목 없음**

### 표: 요소 / 예시 / 크기·굵기 / 색상 / 위치

| ID | 요소 | 예시 | 크기·굵기 | 색상 | 위치 |
|---|---|---|---|---|---|
| V1-P16-001 | 스텝 카운터 | "03 − 04" | 11px Regular | Ink-400 | 화면 우측 상단 |
| **V1-P16-002** | 화면 타이틀 | "선호 액티비티" | **20px Bold** | Ink-900 | 좌측 정렬, 진행바 위 |
| V1-P16-003 | 섹션 라벨 | "선호 카테고리 / 멤버" | 12px Bold | Ink-700 | 좌측 정렬, 그룹 맨 위 |
| V1-P16-004 | 진행 카운터 | "3 / 8" | 12px Bold | Primary | 섹션 라벨과 한 줄, 우측 정렬 |
| V1-P16-005 | 안내문 | "고른 순서가 아니라 개수만 봐요…" | 12.5px Regular | Ink-600 | 섹션 라벨 아래, 좌측 정렬, 최대 2줄 |
| **V1-P16-006** | 항목명(카드·칩 제목) | "성지" | **13.5px Bold** | 미선택 Ink-900 / 선택 White | 카드·칩 상단 |
| **V1-P16-007** | 항목 부가설명 | "뮤비 스팟 · 성지순례" | **11px Regular** | 미선택 Ink-600 / 선택 White 85% | 항목명 바로 아래 |
| **V1-P16-008** | 순위·상태 배지 | "2순위" | **11px Bold** | White(반투명 검정 배경) | 카드 우측 상단 |
| V1-P16-009 | 선택 결과 요약 | "유타(YUTA), 쿤(KUN)… 선택됨" | 12px Regular | Ink-600 | 그리드와 버튼 사이, 좌측 정렬, 줄바꿈 허용 |
| V1-P16-010 | 메뉴명(드롭다운 라벨) | "일정 기준" | 11.5px Medium | Ink-600 | 드롭다운 바로 위 |
| V1-P16-011 | 하단 안내 배너 | "멤버 선택시 멤버별 일정을…" | 11.5px Regular | Ink-600 | 버튼 아래, 화면 최하단 |

| ID | 내용 |
|---|---|
| V1-P16-012 | 확정 사항 박스: ""항목명·부가설명"은 카드/칩이 **선택되면 흰색, 선택 전이면 어두운 톤**으로 자동 전환됩니다. 두 상태 모두 위 크기·굵기 값은 그대로 유지하고 색상만 바뀝니다." |

**코드 반영이 필요한 항목**: V1-P16-006/007/008(13.5px/11px/11px)은 `03_v1_pages_26_43.md`의 V1-P27-006~008과 **동일한 값**(같은 표가 26페이지 근처에도 요약되어 있었음) — `ActivityPreferenceView.module.css`/`PaceView.module.css`의 카드·칩·배지 폰트 크기를 이 값들과 전수 대조 필요.

**판독 불가**: 없음.

---

## 페이지 17 — 여백 & 그리드 (spacing 스케일 + Sticky 원칙) ⚠️ 이번 세션 스크롤 수정과 직결

**상태: 텍스트 확인 완료 · 표 없음(스와치 다이어그램) · 이미지 확인 완료 · 판독 불가 항목 없음**

섹션 "— FOUNDATIONS", 제목 **"여백 & 그리드"**.

| ID | 내용 |
|---|---|
| V1-P17-001 | 설명: "모든 여백·크기는 **4px을 최소값**으로, **8px 배수**를 기본으로 사용합니다." |
| **V1-P17-002** | Spacing 스케일 8단계(막대 그래프 형태): **2**(아이콘 내부) · **4**(텍스트 줄 사이) · **8**(아이콘-텍스트) · **12**(버튼 내부) · **16**(화면 여백·카드) · **20**(카드 패딩(대)) · **24**(요소 그룹 간) · **32**(섹션 간) |
| **V1-P17-003** | 소제목 "상단·하단 고정 레이아웃 (Sticky Header / Footer)": **"모든 전체 화면은 헤더를 화면 상단에, 주요 액션 버튼을 화면 하단에 고정(sticky)합니다. 중간 콘텐츠만 스크롤됩니다."** |

**⚠️ 중요 — 이번 세션 작업의 근거가 되는 원문 규칙**: V1-P17-003이 바로 이번 세션 초반에 진행했던 "스크롤할 때 상단바·하단바가 같이 밀리는 문제 수정" 작업(헤더/푸터를 flex 형제로 분리하고 중간 콘텐츠만 `overflow-y:auto`로 스크롤되게 한 것)의 **정확한 근거 원문**임 — 이미 한 작업이 가이드와 100% 일치함이 이 페이지에서 확인됨.

**코드 반영이 필요한 항목**: spacing 스케일(2/4/8/12/16/20/24/32px)이 각 `.module.css`의 `padding`/`gap`/`margin` 값들과 전수 대조되어 있는지는 이번 조사 범위 밖(값 종류가 매우 많아 스팟체크 권장).

**판독 불가**: 없음.

---

## 페이지 18 — 여백 & 그리드 (헤더/CTA 고정 수치) ⚠️ 최근 헤더 높이 변경과 충돌 가능성

**상태: 텍스트 확인 완료 · 표 확인 완료(7행) · 다이어그램 이미지 확인 완료 · 판독 불가 항목 없음**

다이어그램: 보라 바 "상단 고정·헤더 56px" → 흰 영역 "스크롤 영역(본문 콘텐츠)" → 검정 바 "하단 고정·CTA 버튼".

### 표: 영역 / 기준값

| ID | 영역 | 기준값 |
|---|---|---|
| **V1-P18-001** | 화면 좌우 여백 | **16px** |
| **V1-P18-002** | 헤더 높이 | **56px, 상단 고정** |
| V1-P18-003 | 상태바(시스템 영역) | 헤더가 Primary이므로 아이콘·시간은 항상 흰색(라이트 모드) |
| V1-P18-004 | 하단 CTA 영역 | 하단 고정, 상단 1px 구분선 |
| V1-P18-005 | 카드 radius / 패딩 | 16px / 16~20px |
| V1-P18-006 | 리스트 행 최소 높이 | 56px |
| **V1-P18-007** | 버튼 높이 / radius | **48px / 12px** |

**⚠️ 코드 반영 시 반드시 확인 필요한 충돌 — 헤더 높이가 가이드 수치보다 낮아짐**:
가이드(V1-P18-002)는 헤더 높이를 **정확히 56px**로 못박고 있음. 역산해보면 `AppHeader.module.css`의 예전 값(패딩 상하 14px + 버튼/로고 높이 28px + 패딩 14px = **56px**)이 **정확히 이 규격과 일치**했었음. 그런데 이번 세션 후반부에 사용자가 "상단 젤 윗부분에서 마크까지 길이가 너무 길어" 보인다고 지적해서 `.top-bar` 패딩을 **14px → 10px로 줄였고**, 그 결과 헤더 높이가 **10+28+10 = 48px**로 줄어들어 **가이드 규격(56px)에서 8px 벗어난 상태**가 됨. 사용자의 시각적 피드백(너무 길어 보인다)과 가이드의 명시적 수치(56px)가 서로 다른 방향을 가리키고 있어, **어느 쪽을 기준으로 삼을지 사람이 정해야 함**.

V1-P18-007(버튼 48px/12px)은 `.btn-primary`류의 `min-height` 값들(대부분 52px 사용 중 — 이건 다음 페이지(23p)의 "Large 52px, 하단 고정 CTA" 규격에 해당하는 것으로 보이며, 48px는 "Medium, 일반 버튼" 규격임. 화면 하단 고정 CTA는 52px, 그 외 일반 버튼은 48px로 구분해서 써야 함 — 코드에서 이 구분이 실제로 지켜지고 있는지 전수 확인 필요).

**판독 불가**: 없음.

---

## 페이지 19 — 라운드 & 그림자

**상태: 텍스트 확인 완료 · 표 확인 완료(3개 소표) · 이미지 없음 · 판독 불가 항목 없음**

섹션 "— FOUNDATIONS", 제목 **"라운드 & 그림자"**.

### RADIUS

| ID | 이름 | 값 |
|---|---|---|
| V1-P19-001 | Small (칩·배지) | 20px |
| V1-P19-002 | Medium (버튼·입력필드) | 12px |
| V1-P19-003 | Large (카드·모달) | 16~20px |

### ELEVATION

| ID | 레벨 | 용도 |
|---|---|---|
| V1-P19-004 | Level 0 | 기본 카드 (그림자 없음, 1px 보더) |
| V1-P19-005 | Level 1 | FAB, 드롭다운 |
| V1-P19-006 | Level 2 | 바텀시트 · 모달 |

### BORDER

| ID | 이름 | 값 |
|---|---|---|
| **V1-P19-007** | 기본 구분선 | **1px #ECECF2** |
| V1-P19-008 | 포커스 보더 | 1.5px Primary |

**코드 반영 확인**: `--color-border: #ECECF2` **정확히 일치**. Medium radius 12px은 버튼(`border-radius:12px`류)과 입력필드에서 흔히 쓰이는 것으로 보이며 전수 대조 권장.

**판독 불가**: 없음.

---

## 페이지 20 — 아이콘 (Foundations)

**상태: 텍스트 확인 완료 · 표 없음(아이콘 그리드) · 이미지 확인 완료 · 판독 불가 항목 없음**

섹션 "— FOUNDATIONS", 제목 **"아이콘"**.

| ID | 내용 |
|---|---|
| **V1-P20-001** | 설명: "선형(outline) 스타일 한 종류만 사용합니다. **기본 24×24px, 선 굵기 1.5px.**" |
| **V1-P20-002** | 아이콘 12종 예시(4×3 그리드): **홈**(활성 시 보라 채움 예시) · **일정** · **채팅** · **마이** · **뒤로**(‹) · **추가**(+) · **닫기**(×) · **삭제**(휴지통) · **수정**(연필) · **시간**(시계) · **장소**(핀) · **완료**(체크) |

**코드 반영 확인**: `src/Icon.jsx`의 `PATHS` 객체 키 이름(`home, calendar, chat, person, back, plus, close, trash, edit, clock, pin, check`)이 이 12개 아이콘과 **명칭 그대로 일치**하고, `Icon` 컴포넌트의 기본값 `size=24, strokeWidth=1.5`도 **정확히 일치** — 이 페이지 기준으로는 완전히 가이드를 준수하고 있음.

**판독 불가**: 없음.

---

## 페이지 21 — 헤더·고정 영역 (컴포넌트, squircle 12px 규격)

**상태: 텍스트 확인 완료 · 표 확인 완료(5행) · 예시 이미지 확인 완료 · 판독 불가 항목 없음**

섹션 "— COMPONENTS", 제목 **"헤더·고정 영역"**.

| ID | 내용 |
|---|---|
| V1-P21-001 | 설명: "전체 보라 바 + 중앙 로고 + 원형 뒤로가기 한 가지 형태로 고정하며, 화면 상단에 항상 고정(sticky)됩니다." (※ "원형"이라는 단어가 아래 표의 "원형 아님" 규칙과 문구상 어긋나 보이나, 아래 표가 더 구체적인 확정 규칙이므로 표 기준을 따르는 게 맞아 보임 — 세부 내용은 다음 항목 참고) |
| V1-P21-002 | "기본형(하위 화면)" 예시: 좌측 "‹" 버튼(squircle) + 중앙 "FAN : GO" 텍스트 로고 + 우측 "≡" 버튼(squircle). 캡션: "뒤로가기(좌) + 중앙 로고 + 메뉴 활성화(우), 모두 **라운드 사각(squircle) 12px**" |
| V1-P21-003 | "진입점(홈·로그인)" 예시: 뒤로가기 버튼 없이 로고만 중앙. 캡션: "뒤로가기 없이 로고만 중앙" |

### 표: 좌/우 아이콘 버튼 / 규격

| ID | 항목 | 규격 |
|---|---|---|
| **V1-P21-004** | 모양 | **라운드 사각형(squircle), radius 12px — 원형(circle) 아님** |
| **V1-P21-005** | 크기 | **28×28px** |
| **V1-P21-006** | 배경 | **흰색 22% 불투명(rgba(255,255,255,.22))**, 헤더와 톤온톤 |
| **V1-P21-007** | 아이콘 색 | **흰색, 선형 1.6px** |
| V1-P21-008 | 우측 아이콘 의미 | 탭하면 메뉴(마이페이지·환경설정·히스토리·로그아웃)가 펼쳐지는 트리거다. 사람(프로필) 아이콘은 "내 프로필로 바로 이동"이라는 오해를 주므로 **메뉴 아이콘(≡)으로 사용한다** |
| V1-P21-009 | 예외 박스 | "챗봇(트립 버디) 화면만 아바타+이름+응답상태를 담은 확장형 헤더를 허용합니다." |
| V1-P21-010 | 소제목 "계정 메뉴 드롭다운" 도입: "헤더 우측 메뉴 아이콘(≡)을 누르면 우측 상단에서 펼쳐지는 드롭다운입니다. 사용자 정보 → 메뉴 3개 → 로그아웃 순으로 구분선을 둡니다. "마이페이지"는 이 메뉴 안의 항목 중 하나일 뿐이므로, 트리거 아이콘 자체를 프로필/사람 모양으로 쓰지 않습니다." |

**코드 반영이 필요한 항목**: `AppHeader.module.css`의 `.top-bar-btn`이 **width/height 28px**는 일치하는 것으로 기억되나, radius(12px squircle), 배경(rgba(255,255,255,.22)), 아이콘 스트로크(1.6px — `Icon.jsx` 기본값은 1.5px이라 헤더에서 별도로 1.6px를 지정하고 있는지 확인 필요) 값들을 전수 대조 필요. 이번 세션에서 헤더 패딩을 줄인 작업(14px→10px)이 있었지만 버튼 자체의 크기(28×28px)·모양(squircle 12px)은 건드리지 않았으므로 그 부분은 영향 없을 것으로 추정.

**판독 불가**: 없음.

---

## 페이지 22 — 계정 메뉴 드롭다운 (상세)

**상태: 텍스트 확인 완료 · 표 확인 완료(4행) · 예시 이미지 확인 완료 · 판독 불가 항목 없음**

예시 이미지: "test01 / test01@gmail.com" 사용자 정보 + "마이페이지"(프로필 및 계정 관리) · "환경설정"(알림·언어·테마) · "여행 히스토리"(지난 동선·별점) · 구분선 · "로그아웃"(현재 세션 종료) — **`01_v1_pages_01_10.md`의 V1-P03-001 BEFORE 캡처와 동일한 화면**(컬러 아이콘 사용 중인 상태).

### 표: 규칙 / 내용

| ID | 규칙 | 내용 |
|---|---|---|
| V1-P22-001 | 구조 | 사용자 정보 → 구분선 → 메뉴 3개(마이페이지·환경설정·여행 히스토리) → 구분선 → 로그아웃 |
| **V1-P22-002** | 행 구성 | **아이콘(34px, radius 10px)** + 제목(Bold) + 부제목(Caption) + chevron(›) |
| **V1-P22-003** | 아이콘 색상 | 현재 컬러 아이콘 사용 중 → **05 아이콘 규칙(선형 단색)에 맞춰 통일 필요** · 수정안 참고 |
| V1-P22-004 | 로그아웃 | 구분선으로 분리해 실수 클릭을 방지한다 |

**코드 반영 확인**: `AppHeader.jsx`의 드롭다운 메뉴는 이미 `Icon` 컴포넌트(선형 단색, `person`/`gear`/`plane`/`logout`)를 쓰고 있어 V1-P22-003의 "통일 필요" 지적이 **이미 해결된 상태**로 보임(01 문서의 V1-P03-001에서도 동일하게 확인함). 아이콘 컨테이너 34px/radius 10px 값은 전수 대조 권장.

**판독 불가**: 없음.

---

## 페이지 23 — 버튼 (컴포넌트) ⚠️ "다음:" 버튼 충돌의 핵심 원본

**상태: 텍스트 확인 완료 · 표 확인 완료(1개) · Before/After 이미지 확인 완료 · 판독 불가 항목 없음**

섹션 "— COMPONENTS", 제목 **"버튼"**.

| ID | 내용 |
|---|---|
| V1-P23-001 | 부제: "버튼은 "행동"만 담고, 정보는 분리 — 확정" |
| **V1-P23-002** | **BEFORE**: "2박 · 이 숙소로 추가 →"(정보+행동+화살표가 한 버튼에 섞임), 하단 3버튼 — "목록 열기"(outline) / "동선 수정"(outline) / **"다음: 확인 →"**(Primary, 2줄로 줄바꿈되어 화살표가 다음 줄에 표시됨) |
| **V1-P23-003** | **AFTER·확정**: "2박 · 하이엔 인사이트"(버튼 밖 캡션으로 분리), "숙소로 추가"(Primary 단독 버튼), 하단 2버튼 — "동선 수정"(outline) / **"확인"**(Primary) |
| **V1-P23-004** | 확정 사항 박스: ""목록 열기"는 자주 쓰이지 않는 보조 동작이라 하단에서 **완전히 제거**합니다(다른 위치로 옮기지 않음). "다음"과 "확인"은 같은 동작(다음 단계로 진행)을 반복하는 것이었으므로 **별도의 "다음" 버튼도 제거**하고, "확인"이 곧 다음 단계로 진행하는 버튼입니다. 하단엔 **동선 수정·확인** 두 가지만 남기고, 정보(2박·숙소명)는 버튼이 아닌 캡션 텍스트로 뺍니다." |

### "종류 & 상태 (화살표 미사용)" 예시

| ID | 내용 |
|---|---|
| V1-P23-005 | "확인"(Primary 채움) / "동선 수정"(연보라 Secondary) / "목록 열기"(Outline) / **"지도에서 장소를 골라주세요"**(비활성, 연보라 톤 + 흐린 텍스트) / "자세히 보기"(텍스트 링크) |

### 표: 크기 / 높이 / 용도

| ID | 크기 | 높이 | 용도 |
|---|---|---|---|
| **V1-P23-006** | Large | **52px** | 하단 고정 CTA |
| **V1-P23-007** | Medium | **48px** | 일반 버튼 |
| **V1-P23-008** | Small | **36px** | 리스트 행 내부 |

**⚠️ 이 페이지가 보여주는 것은 "다음:" 형식 자체의 금지가 아니라, 이 특정 화면에서 "다음"과 "확인"이 같은 동작을 하는 중복 버튼이었다는 문제**임 — 즉 V1-P23-002/004는 "콜론(:) 형식을 없애라"는 일반 규칙이 아니라 "이 화면에서 중복되는 버튼 하나(다음)를 통째로 지우고 남은 버튼(확인)을 쓰라"는 **개별 사례의 정리**로 읽힘. 일반적인 화살표·콜론 규칙은 다음 페이지(24p)에 별도로 명시되어 있고, 그 규칙과 함께 보면 의미가 더 명확해짐 — 아래 "종합 요약"에서 통합 정리.

V1-P23-006(Large 52px)은 코드베이스 대다수 화면의 `.btn-primary { min-height: 52px }`와 **정확히 일치**. V1-P23-005의 비활성 버튼 예시("지도에서 장소를 골라주세요")는 실제 `StaySearchView.jsx`/`TripDateView.jsx`의 문구와 **일치**.

**판독 불가**: 없음.

---

## 페이지 24 — 버튼 규칙표(화살표·라벨·비활성) + 입력 필드·드롭다운 시작

**상태: 텍스트 확인 완료 · 표 확인 완료(2개) · 예시 이미지 확인 완료 · 판독 불가 항목 없음**

### 표: 규칙 / 내용 (06 버튼 — 일반 규칙)

| ID | 규칙 | 내용 |
|---|---|---|
| **V1-P24-001** | **화살표(→)** | **모든 버튼에서 사용하지 않는다. "다음: 액티비티"처럼 콜론(:)으로 다음 단계를 안내한다** |
| V1-P24-002 | 라벨 | 동사+목적어 2~4어절, 명사형으로 끝내지 않는다 |
| **V1-P24-003** | 비활성 상태 | 조건 미충족 시 옅은 톤(**#D9D4F5**)으로 확실히 구분한다 |
| V1-P24-004 | 정보와 행동 분리 | 가격·기간 등 정보는 버튼 밖 캡션으로, 버튼엔 행동만 |
| **V1-P24-005** | 하단 버튼 그룹 개수 | 화면 하단 고정 영역엔 **최대 2개까지만** 나란히 놓는다. 비율은 보조:주요 = **40:60 또는 50:50** |
| V1-P24-006 | 중복 라벨 금지 | 한 화면 안에서 같은 라벨의 버튼을 서로 다른 범위(scope)에 중복 배치하지 않는다. 대상이 다르면 라벨도 다르게 쓴다 (예: 행 안의 "수정"과 화면 하단 "동선 저장") |

**⚠️ 이 표(V1-P24-001)가 "다음:" 버튼 논쟁의 최종 원본 규칙**: 화살표(→)는 모든 버튼에서 금지하지만, **"다음: 액티비티"처럼 콜론(:)+다음 화면명 형식 자체는 명시적으로 허용(오히려 권장 예시로 제시됨)**. `03_v1_pages_26_43.md`에서 페이지 39와 40·43이 서로 다른 말을 하는 것처럼 보였던 부분이, 여기 24p 원본 규칙까지 같이 보면 다음과 같이 정리됨:
- **일반 규칙(24p, 40p, 43p)**: 화살표만 금지, "다음: 화면명"(콜론 형식)은 표준적으로 허용.
- **23p의 특정 사례**: 그 화면에 한해서는 "다음: 확인"이 이미 있는 "확인" 버튼과 **동작이 100% 중복**되는 문제였어서, 콜론 형식이 아니라 **버튼 자체를 통째로 제거**한 것 — 일반 규칙에 대한 예외라기보다는 "중복 버튼 제거"라는 별개의 문제였던 것으로 해석하는 게 문서 전체와 가장 잘 맞음.
- 즉 **결론적으로 "다음: 화면명" 형식은 원칙적으로 허용**되며(화살표만 빼고), 이번 세션에서 실제로 진행한 작업(EventSelectView·ActivityPreferenceView의 "다음:" 접두사를 완전히 제거)은 **콜론 형식 자체를 없앤 것이라 가이드의 일반 규칙과는 다른 방향**임 — 다만 두 화면 다 애초에 "다음: 대상+화살표" 조합에서 **화살표를 제거**했다는 점은 가이드와 일치하고, "콜론+대상명"까지 지울지는 선택의 문제로 남음.

V1-P24-003(비활성 #D9D4F5)은 이번 세션에서 사용자가 명시적으로 확정한 값("연보라(#D9D4F5)+흰글자로 커밋")과 **정확히 일치** — 그 결정이 가이드 원문과 100% 부합했음이 이 페이지에서 최종 확인됨.

### "입력 필드 · 드롭다운" 컴포넌트 시작

| ID | 내용 |
|---|---|
| V1-P24-007 | 예시: "이메일"(기본, placeholder "mina_tan@google.com"), "비밀번호"(Focus 상태, 보라 테두리), "닉네임"(Error 상태, 빨강 테두리 + 하단 "이미 사용 중이에요"), "국가"(Disabled, 회색 배경, "대한민국(변경불가)") |

### 표: 상태 / 보더

| ID | 상태 | 보더 |
|---|---|---|
| V1-P24-008 | 기본 | 1.5px #ECECF2 |
| V1-P24-009 | Focus | 1.5px Primary + Glow |
| V1-P24-010 | Error | 1.5px Danger + 하단 안내(적색) |
| **V1-P24-011** | Disabled | 배경 **#F6F6F9** |

**판독 불가**: 없음.

---

## 페이지 25 — 배지 & 칩 (컴포넌트)

**상태: 텍스트 확인 완료 · 표 확인 완료(1개) · 예시 이미지 확인 완료 · 판독 불가 항목 없음**

섹션 "— COMPONENTS", 제목 **"배지 & 칩"**.

| ID | 내용 |
|---|---|
| V1-P25-001 | "상태 배지" 예시: "진행 중"(outline) · "완료"(회색 채움) · "여유 92"(초록) · "보통 65"(주황) · "혼잡 30"(빨강) · "고정 ✓"(보라 채움) |
| V1-P25-002 | "선택 칩(멤버·필터)" 예시: "해찬(HAECHAN)"(선택, 보라 채움) · "재민(JAEMIN)" · "양양(YANGYANG)" · "런쥔(RENJUN)"(모두 outline) |
| V1-P25-003 | 소제목 "다중 인원(멤버) 선택 — 확정": "멤버가 10명을 넘는 그룹은 칩이 길게 이어지므로, 목록·문구·버튼 배치를 아래 형태 하나로 고정합니다." |
| V1-P25-004 | 예시: "전체(선택 안 함)"(맨 앞 칩, outline) · "사쿠야" · "해찬"(선택, 채움) · "재민" · "양양" · "런쥔", 아래 "해찬(HAECHAN), 재민(JAEMIN) 선택됨" 요약 텍스트, "초기화"(outline)/"확인"(채움) 버튼, 안내 배너 "ⓘ 멤버를 선택하면 그 멤버 일정을 우선 추천받아요." |

### 표: 요소 / 규칙

| ID | 요소 | 규칙 |
|---|---|---|
| V1-P25-005 | 목록 형태 | 2열 칩 그리드, 리스트가 길면 **바텀시트 내부만 스크롤**(헤더·버튼은 고정) |
| **V1-P25-006** | "전체/선택 안 함" | 목록 **맨 앞에 칩으로** 넣는다. 다른 텍스트 링크로 따로 만들지 않는다 |
| V1-P25-007 | 선택 상태 | 다중 선택 — 다른 멤버 칩은 동시에 여러 개 선택 가능. "전체" 칩을 고르면 나머지는 자동 해제 |
| V1-P25-008 | 안내 문구 | 선택이 결과에 어떤 영향을 주는지 1줄로, 그리드와 버튼 사이에 배치 |
| V1-P25-009 | 하단 버튼 | 초기화(outline) + 확인(filled), 06 버튼 규칙과 동일하게 2개만 |
| V1-P25-010 | 확정 사항 박스(26p로 이어짐): "이전 시안엔 칩 목록 아래 "선택 안 함" 텍스트 링크와 "초기화" 버튼이 함께 있어 같은 의미(선택 취소)가 두 곳에 있었습니다. "선택 안 함"은 별도 링크로 두지 않고 목록 맨 앞의 칩 하나로 흡수합니다. "초기화"는…" (26페이지에서 이어짐, `03_v1_pages_26_43.md`의 V1-P26 항목들과 연결됨) |

**코드 반영 확인**: `PaceView.jsx`의 "전체(선택 안 함)" 칩 + 멤버 칩 그리드 + "초기화"/"확인" 버튼 + 안내 배너 구조가 이 페이지의 확정안과 **정확히 일치**하는 것으로 보임(세션 이력상 이 패턴을 참고해 이미 구현됨).

**판독 불가**: 없음.

---

## 종합 요약 (11~25페이지)

### 코드 반영이 이미 되어 있는 것으로 확인된 항목 (정확히 일치)

- **색상 토큰 전부**: `index.css`의 `--color-success/#2E9E5B`, `--color-warning/#E5A02E`, `--color-danger/#E5484D`, `--color-info/#2E7CE9`, `--color-ink-900/#17161F`, `--color-ink-600/#5C5D6E`, `--color-ink-400/#93949F`, `--color-ink-200/#E2E2E9`, `--color-border/#ECECF2` — **9개 전부 원본 hex와 정확히 일치**
- `PickerSheet.module.css`의 다크시트 배경 `#2B2A3A` — 일치 (2번째 재확인)
- 비활성 버튼 `#D9D4F5` — 이번 세션에 사용자가 확정한 값과 가이드 원문이 **정확히 일치** (V1-P24-003)
- `Icon.jsx`의 12개 아이콘 이름 + `size=24, strokeWidth=1.5` 기본값 — 완전 일치 (V1-P20-001/002)
- 페이지 17의 Sticky Header/Footer 원칙 — 이번 세션 스크롤 버그 수정 작업의 정확한 근거
- 하단 CTA 버튼 `min-height: 52px` (Large) — 코드 대다수 화면과 일치 (V1-P23-006)
- `PaceView.jsx`의 멤버 선택 칩·초기화/확인 버튼 구조 — 25p 확정안과 일치
- `AppHeader.jsx` 드롭다운 메뉴 아이콘(선형 단색) — 22p 지적사항이 이미 해결된 상태

### ⚠️ 새로 발견한 충돌/확인 필요 사항

1. **V1-P14-001~003 — 폰트 패밀리 자체가 다름**: 가이드는 "맑은 고딕(Malgun Gothic)" + "Apple SD Gothic Neo"(둘 다 시스템 폰트)를 지정하지만, 실제 코드는 Google Fonts(IBM Plex Sans KR·Urbanist·Inter)를 쓰고 있음 — 수치 차이가 아니라 **폰트 자체가 다른** 근본적인 불일치.
2. **V1-P18-002 — 헤더 높이 56px vs 이번 세션에 48px로 줄인 것**: 가이드는 헤더 높이를 정확히 56px로 못박고 있고, 예전 코드(패딩 14px)는 정확히 이 값을 만족했으나, 이번 세션에서 사용자의 "너무 길어 보인다"는 피드백에 따라 패딩을 10px로 줄이면서 48px가 되어 가이드 수치에서 8px 벗어남.
3. **V1-P24-001 — "다음:" 버튼 형식의 재해석**: 26~43페이지 분석에서 "문서 내부 모순"으로 플래그했던 사안이, 이 페이지(24p)의 원본 규칙("화살표만 금지, 콜론+화면명은 허용")까지 함께 보면 **모순이 아니라 일반 규칙 + 개별 사례의 조합**으로 재해석됨. 다만 이번 세션에서 실제로 한 작업(콜론+대상명까지 완전히 제거)은 이 일반 규칙보다 더 강하게 축약한 것이라, 가이드에 맞추려면 "다음: 기간·숙소"/"다음: 동선 스타일"처럼 콜론 형식을 되살리는 선택지도 있음 — 최종 판단은 사람 몫으로 남김.

### 페이지별 상태 총괄

| 페이지 | 텍스트 | 표 | 이미지 | 판독 불가 |
|---|---|---|---|---|
| 11 | 확인 완료 | 해당 없음 | 해당 없음 | 없음 |
| 12 | 확인 완료 | 해당 없음(카드형) | 확인 완료 | Primary Scale 8단계 hex 값(스와치엔 색만 있고 텍스트 hex 없음) |
| 13 | 확인 완료 | 확인 완료 | 확인 완료 | 없음 |
| 14 | 확인 완료 | 확인 완료 | 확인 완료(타입 예시) | 없음 |
| 15 | 확인 완료 | 해당 없음(카드형) | 해당 없음 | 없음 |
| 16 | 확인 완료 | 확인 완료 | 해당 없음 | 없음 |
| 17 | 확인 완료 | 해당 없음(다이어그램) | 확인 완료 | 없음 |
| 18 | 확인 완료 | 확인 완료 | 확인 완료(다이어그램) | 없음 |
| 19 | 확인 완료 | 확인 완료 | 해당 없음 | 없음 |
| 20 | 확인 완료 | 해당 없음(아이콘 그리드) | 확인 완료 | 없음 |
| 21 | 확인 완료 | 확인 완료 | 확인 완료 | 없음 |
| 22 | 확인 완료 | 확인 완료 | 확인 완료 | 없음 |
| 23 | 확인 완료 | 확인 완료 | 확인 완료 | 없음 |
| 24 | 확인 완료 | 확인 완료 | 확인 완료 | 없음 |
| 25 | 확인 완료 | 확인 완료 | 확인 완료 | 없음 |

**전체 결론**: 11~25페이지는 가이드 문서의 핵심인 **Foundations(컬러·타이포·여백·라운드·아이콘)**와 **Components 앞부분(헤더·버튼·배지)**을 담고 있어, 26~43페이지 분석에서 "확인 필요"로만 남겨뒀던 정확한 hex·px 수치의 **원본 출처**를 대부분 찾아 채웠음. 유일한 판독 불가 항목은 Primary Scale(50~900) 8단계의 정확한 hex 코드(스와치 이미지에는 색만 있고 텍스트 라벨이 없음) — 이 값은 코드의 `index.css` `--color-primary-*` 정의를 기준으로 삼는 것을 권장. 가장 중요한 실질적 발견은 **폰트 패밀리 불일치(맑은 고딕 vs Google Fonts)**와 **헤더 높이 56px 규정과 이번 세션 조정(48px)의 차이** 두 가지이며, 둘 다 사람의 결정이 필요한 사안으로 남겨둠.
