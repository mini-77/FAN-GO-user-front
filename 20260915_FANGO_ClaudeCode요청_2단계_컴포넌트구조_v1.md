# Claude Code 요청 — 2단계: 컴포넌트별 구조 변경

작성일: 2026-09-15
범위: 실제 마크업·로직 수정이 필요한 항목 4개. **1단계(토큰 값 교체)가 끝난 뒤에 진행하는 것을 권장하며, 아래 4개를 한 번에 몰아서 요청하지 말고 하나씩 순서대로 요청하세요.** 각 항목 사이에 구분선(`---`)을 넣어뒀으니, 요청할 때는 해당 항목 블록만 복사해서 보내면 됩니다.

---

## 2-1. 버튼 라벨 콜론 접두어 제거

기존엔 다음/이전 화면으로 이동하는 버튼에 "다음: 액티비티", "이전: 확인"처럼 콜론(:) 접두어를 붙이는 규칙이 있었는데, **이 규칙을 완전히 폐기**했습니다. 이제는 접두어 없이 이동할 화면의 메뉴명만 그대로 씁니다.

**요청 문구:**
우리 프로젝트 코드에서 버튼 라벨에 "다음:", "이전:" 접두어나 화살표(→)를 붙이는 곳을 전부 찾아줘. 찾은 목록을 먼저 보여주고, 확인 후 접두어를 제거하고 이동할 화면명만 남기는 방식으로 수정해줘 (예: "다음: 확인 →" → "확인", "이전: 행사 선택" → "행사 선택"). 화면 로직(라우팅 등)은 건드리지 말고 라벨 텍스트만 수정해줘.

---

## 2-2. 안내문(Notice) 배경 제거

안내문 컴포넌트에 박스형 배경(예: 옅은 파란 배경)이 있었는데, 실제 화면엔 없던 요소라 **삭제**했습니다. 이제는 화면과 동일한 흰 배경 + 아이콘 색상만으로 톤(일반/경고)을 구분하고, 텍스트는 항상 Ink-600(`#5C5D6E`)입니다. 필요하면 위·아래 1px 구분선만 허용합니다.

**요청 문구:**
우리 프로젝트에서 안내문(notice/info banner) 컴포넌트를 찾아줘. 배경색이 박스 형태로 들어가 있다면 제거하고, 배경을 투명(화면과 동일)하게 바꿔줘. 텍스트 색상은 상황(일반/경고)과 무관하게 항상 Ink-600(`#5C5D6E`)으로 통일하고, 아이콘 색상만 Info(`#2E7CE9`)/Warning(`#E5A02E`) 등 상황별로 다르게 유지해줘. 수정 전에 영향받는 파일 목록을 먼저 알려줘.

---

## 2-3. 언어 선택 UI 구조 변경

언어 선택을 다크 바텀시트(하단에서 올라오는 어두운 배경 시트)로 구현했다면, **헤더 "KR" 필 바로 아래 우측에 앵커되는 흰색 드롭다운**으로 구조를 바꿔야 합니다. 언어 목록도 3개(한국어/English/日本語)에서 **6개(+中文/ภาษาไทย/Bahasa Indonesia)**로 늘었고, 선택 표시(라디오 버튼 등)는 없앴습니다 — 탭하면 즉시 전환되므로 별도 표시가 불필요합니다. 현재 언어만 텍스트 색상(Primary, `#6D57FC`)으로 구분합니다.

**요청 문구:**
우리 프로젝트에서 언어 선택(language picker) 관련 컴포넌트를 찾아줘. 찾은 구현 방식(다크 바텀시트인지, 이미 드롭다운인지)을 먼저 알려줘. 다크 바텀시트라면 헤더의 "KR" 필 버튼 바로 아래·우측에 앵커되는 흰색 드롭다운 메뉴로 구조를 바꿔줘 (뒤 화면은 살짝 어둡게 dim 처리). 언어 목록에 中文(중국어)·ภาษาไทย(태국어)·Bahasa Indonesia(인도네시아어)가 빠져 있다면 추가해줘. 선택 상태를 라디오 버튼이나 체크 아이콘으로 표시하고 있다면 제거하고, 현재 선택된 언어만 텍스트 색상을 Primary로 표시하도록 바꿔줘. 구조 변경 전에 영향받는 파일을 먼저 알려주고 확인 후 진행해줘.

---

## 2-4. 리스트 행 제목 말줄임 처리

리스트에서 제목이 긴 항목의 경우, 옆에 붙는 배지("준비 중" 등)가 줄바꿈되며 깨지는 버그가 있었습니다. 리스트 행 제목은 **1줄로 고정**하고 넘치면 말줄임(…) 처리, 배지·보조 텍스트는 **항상 우측에 고정폭으로 배치**해서 줄바꿈되지 않게 해야 합니다.

**요청 문구:**
우리 프로젝트에서 리스트 행(이벤트 목록, 저장한 동선 목록 등) 컴포넌트를 찾아줘. 제목이 길 때 옆의 배지가 함께 줄바꿈되는 CSS 버그가 있는지 확인해줘. 있다면, 제목 텍스트는 1줄로 고정하고 넘치면 말줄임(text-overflow: ellipsis)으로 처리하고, 배지는 `flex-shrink: 0`으로 고정폭 유지하며 줄바꿈되지 않게 수정해줘. 수정 전에 영향받는 파일을 먼저 알려줘.
